#include <stdio.h>
#include <string.h>

int main() {
    char str1[] = "dd";
    char str2[] = "ce";

    if (strcmp(str1, str2) == 0) {
        printf("指定された文字列は同じです。\n");
    } else {
        printf("指定された文字列は異なります。\n");

        jin
    }

    return 0;
}